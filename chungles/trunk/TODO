*(optional) multicast transfers
*(optional) gzip compression on line (default: no)
*(optional) ssl encryption (default: no)
*user authentication
*coolbar with operation buttons
*more file operations (del,move,copy,etc)
*file resuming
*search feature
*separate core functionality, set up packages (crucial) - this means having classes that do core chungles functionality and then many things can use it
*integrate stateless
